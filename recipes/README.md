# description 
- tested if the supabase authentification and profile tables (auth automation)is working ==> when an acount is created , a user profile will be automatically created (new row in the profiles table)

- created two example login/register pages to test the login/register 

==> login and register working